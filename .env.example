# Build-time public config only (no secrets)
VITE_APP_NAME=Pfad des Paradoxons
VITE_ENVIRONMENT=development

# Optional relative API prefix, e.g. /api
# Keep empty for same-origin root routing via gateway.
VITE_API_BASE_PATH=
